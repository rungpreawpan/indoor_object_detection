name: indoor_object_detection
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  intl: ^0.20.2
  dio: ^5.8.0+1
  connectivity_plus: ^6.1.4
  google_fonts: ^6.3.0
  freezed_annotation: ^3.1.0
  camera: ^0.11.2
  image_picker: ^1.1.2
  flutter_svg: ^2.2.0
  photo_manager: ^3.7.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  freezed: ^3.1.0
  build_runner: ^2.5.4

flutter:
  uses-material-design: true

  assets:
    - assets/icons/